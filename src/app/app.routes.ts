import { Routes } from '@angular/router';
import { VoiceButtonComponent } from './components/voice-button/voice-button.component';

export const routes: Routes = [
    { path: "", component: VoiceButtonComponent }
];
